---
import Logo from './Logo.astro';
import Icon from './Icon.astro';
import { navPrincipal, site, whatsappHref } from '../data/site';

const currentPath = Astro.url.pathname;
const isActive = (href: string) => currentPath === href || (href !== '/' && currentPath.startsWith(href));
---

<header class="sticky top-0 z-50 border-b border-navy/10 bg-white/95 backdrop-blur print:hidden">
  <div class="container-page flex h-16 items-center justify-between gap-4 sm:h-20">
    <a href="/" class="flex shrink-0 items-center gap-2">
      <Logo class="h-11 w-auto sm:h-12" />
      <span class="sr-only">{site.nombreCorto} — inicio</span>
    </a>

    <nav aria-label="Navegación principal" class="hidden lg:block">
      <ul class="flex items-center gap-1 text-[0.95rem] font-medium text-navy">
        {navPrincipal.map((item) => (
          <li class="group relative">
            <a
              href={item.href}
              aria-current={isActive(item.href) ? 'page' : undefined}
              class={`flex items-center gap-1 rounded-md px-3 py-2 transition hover:text-teal-dark ${isActive(item.href) ? 'text-teal-dark' : ''}`}
            >
              {item.label}
              {item.children && <Icon name="chevron-down" class="h-3.5 w-3.5 opacity-60" />}
            </a>
            {item.children && (
              <ul class="invisible absolute left-0 top-full z-10 w-64 -translate-y-1 rounded-xl border border-navy/10 bg-white p-2 opacity-0 shadow-cardHover transition duration-150 group-hover:visible group-hover:translate-y-0 group-hover:opacity-100 group-focus-within:visible group-focus-within:translate-y-0 group-focus-within:opacity-100">
                {item.children.map((child) => (
                  <li>
                    <a href={child.href} class="block rounded-lg px-3 py-2 text-sm text-navy hover:bg-mist">
                      {child.label}
                    </a>
                  </li>
                ))}
              </ul>
            )}
          </li>
        ))}
      </ul>
    </nav>

    <div class="flex items-center gap-2">
      <button
        id="theme-toggle"
        type="button"
        class="rounded-full p-2 text-navy transition hover:bg-mist"
        aria-label="Cambiar entre modo claro y oscuro"
      >
        <Icon name="moon" class="hidden h-5 w-5 dark:block" />
        <Icon name="sun" class="block h-5 w-5 dark:hidden" />
      </button>

      <a href={whatsappHref()} target="_blank" rel="noopener noreferrer" class="btn-whatsapp hidden sm:inline-flex" data-analytics-event="clic_whatsapp" data-analytics-origin="header">
        <Icon name="whatsapp" class="h-4 w-4" />
        <span class="hidden md:inline">WhatsApp</span>
      </a>
      <a href="/contacto/" class="btn-primary hidden sm:inline-flex">
        Agendar diagnóstico
      </a>

      <button
        id="mobile-menu-button"
        type="button"
        class="rounded-md p-2 text-navy hover:bg-mist lg:hidden"
        aria-expanded="false"
        aria-controls="mobile-menu"
        aria-label="Abrir menú de navegación"
      >
        <Icon name="menu" class="h-6 w-6" />
      </button>
    </div>
  </div>

  <div id="mobile-menu" class="hidden border-t border-navy/10 bg-white px-5 pb-6 pt-2 lg:hidden">
    <nav aria-label="Navegación móvil">
      <ul class="flex flex-col divide-y divide-navy/10">
        {navPrincipal.map((item) => (
          <li class="py-1">
            <a href={item.href} class="block rounded-md py-2.5 font-medium text-navy">{item.label}</a>
            {item.children && (
              <ul class="ml-3 flex flex-col border-l border-navy/10 pl-3">
                {item.children.map((child) => (
                  <li><a href={child.href} class="block py-2 text-sm text-ink-soft">{child.label}</a></li>
                ))}
              </ul>
            )}
          </li>
        ))}
      </ul>
    </nav>
    <div class="mt-4 flex flex-col gap-2">
      <a href={whatsappHref()} target="_blank" rel="noopener noreferrer" class="btn-whatsapp" data-analytics-event="clic_whatsapp" data-analytics-origin="menu_movil">
        <Icon name="whatsapp" class="h-4 w-4" /> Escribir por WhatsApp
      </a>
      <a href="/contacto/" class="btn-primary">Agendar diagnóstico inicial</a>
    </div>
  </div>
</header>

<script>
  const menuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');

  menuButton?.addEventListener('click', () => {
    const isOpen = mobileMenu?.classList.toggle('hidden') === false;
    menuButton.setAttribute('aria-expanded', String(isOpen));
    menuButton.setAttribute('aria-label', isOpen ? 'Cerrar menú de navegación' : 'Abrir menú de navegación');
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && mobileMenu && !mobileMenu.classList.contains('hidden')) {
      mobileMenu.classList.add('hidden');
      menuButton?.setAttribute('aria-expanded', 'false');
      menuButton?.focus();
    }
  });

  const themeToggle = document.getElementById('theme-toggle');
  themeToggle?.addEventListener('click', () => {
    const root = document.documentElement;
    const isDark = root.classList.toggle('dark');
    localStorage.setItem('aic-theme', isDark ? 'dark' : 'light');
  });
</script>
